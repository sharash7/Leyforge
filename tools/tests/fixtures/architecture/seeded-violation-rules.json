{
  "schema_version": "prd07-architecture-rules-v1",
  "scan_roots": ["bad"],
  "rules": [
    {"rule_id": "TEST-ARCH-001", "source_prefix": "bad.domain", "forbidden_prefix": "provider.internal", "reason": "Domain cannot reach provider internals."}
  ]
}
