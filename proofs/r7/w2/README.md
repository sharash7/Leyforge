# Leyforge R7 W2 proof runtime

This nested project and Python fixture package implement only the governed PRD-07 W2 network, persistence, recovery, migration and real-crash proof surfaces. They consume the certified W0 dependency lock and W1 operation concepts without changing either evidence set.

The Godot project under `server_probe/` is an exported proof process. It is not a production game project and does not open R8. The Python controller externally terminates only disposable instances created beneath the ignored `.local` execution root.

W2 load figures are experimental. PROOF-57/58 retain the full FCC-13E 312-row conformance rerun for W4.
